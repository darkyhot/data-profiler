# Ядро (нужно всегда, работает на закрытом контуре)
SQLAlchemy>=2.0
pandas>=2.0
psycopg2-binary>=2.9
requests>=2.28

# LLM-вариант "qwen" (langchain GigaChat). Не нужен для вариантов "glm"/"openai".
# langchain-gigachat>=0.3
